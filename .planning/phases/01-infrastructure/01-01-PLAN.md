---
phase: 01-infrastructure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .shellcheckrc
  - .editorconfig
  - hardnormly.sh
autonomous: true

must_haves:
  truths:
    - "Running shellcheck hardnormly.sh exits 0 with no warnings"
    - "Running shfmt -d -i 0 -bn -ci hardnormly.sh exits 0 (no diffs)"
    - ".shellcheckrc configures shell=bash and enables optional checks"
    - ".editorconfig enforces tab indentation for shell files"
  artifacts:
    - path: ".shellcheckrc"
      provides: "ShellCheck configuration"
      contains: "shell=bash"
    - path: ".editorconfig"
      provides: "Editor config for consistent formatting"
      contains: "indent_style = tab"
    - path: "hardnormly.sh"
      provides: "Main script with zero ShellCheck warnings and consistent shfmt formatting"
  key_links:
    - from: ".shellcheckrc"
      to: "hardnormly.sh"
      via: "shellcheck reads config automatically from repo root"
      pattern: "shell=bash"
---

<objective>
Add ShellCheck config, fix all existing ShellCheck findings, and apply shfmt formatting to hardnormly.sh.

Purpose: Establish the linting and formatting baseline so all subsequent Phase 1 work (strict mode, CI, hooks) builds on a clean foundation. This plan covers requirements INFR-01 (ShellCheck) and INFR-02 (shfmt/editorconfig).

Output: `.shellcheckrc`, `.editorconfig`, and a reformatted `hardnormly.sh` that passes both `shellcheck` and `shfmt -d` with zero findings.
</objective>

<execution_context>
@C:\Users\bernt\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\bernt\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-infrastructure/01-CONTEXT.md
@.planning/phases/01-infrastructure/01-RESEARCH.md
@hardnormly.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create config files and fix all ShellCheck findings</name>
  <files>.shellcheckrc, .editorconfig, hardnormly.sh</files>
  <action>
1. Create `.shellcheckrc` in repo root with these options (from research Pattern 1):
   ```
   shell=bash
   enable=require-double-brackets
   enable=deprecate-which
   enable=check-set-e-suppressed
   external-sources=true
   ```

2. Create `.editorconfig` in repo root:
   ```
   root = true

   [*]
   end_of_line = lf
   insert_final_newline = true
   trim_trailing_whitespace = true
   charset = utf-8

   [*.sh]
   indent_style = tab
   indent_size = 4

   [*.{yml,yaml}]
   indent_style = space
   indent_size = 2

   [Makefile]
   indent_style = tab
   ```

3. Run `shellcheck hardnormly.sh` and enumerate all findings. The script currently has approximately 5 known findings (per PROJECT.md). Expected issues include:
   - SC2155: `local timestamp=$(date ...)` at line 74 — fix by declaring `local timestamp` separately, then `timestamp=$(date ...)`
   - SC2294: `eval "$pipeline_cmd"` at line 401 — do NOT fix here; this is addressed in Plan 02 (eval replacement). For now, add a single temporary `# shellcheck disable=SC2294` on the eval line ONLY. This is the one exception to the zero-disable policy because the fix requires the structural refactoring in Plan 02. Document this exception in a code comment.
   - Any SC2086 (unquoted variable) findings — fix by quoting variables
   - Any other findings — fix each one directly, no inline disables

4. IMPORTANT: Do NOT add `set -euo pipefail` or change the trap. That is Plan 02's scope. Do NOT restructure the filter pipeline. Only fix ShellCheck findings that can be fixed without structural changes.

5. After fixing, run `shellcheck hardnormly.sh` again and confirm exit code 0.
  </action>
  <verify>
Run `shellcheck hardnormly.sh` — must exit 0 with no output (except the temporary SC2294 disable on the eval line, which is acceptable as a documented exception until Plan 02 removes eval entirely).
Run `cat .shellcheckrc` to confirm file exists with correct content.
Run `cat .editorconfig` to confirm file exists.
  </verify>
  <done>shellcheck hardnormly.sh exits 0. .shellcheckrc and .editorconfig exist with correct content. All ShellCheck findings are fixed except SC2294 (eval) which has a documented temporary disable.</done>
</task>

<task type="auto">
  <name>Task 2: Apply shfmt formatting to hardnormly.sh</name>
  <files>hardnormly.sh</files>
  <action>
1. Run `shfmt -d -i 0 -bn -ci hardnormly.sh` to see the diff of what shfmt would change. Review the diff to understand scope of changes.

2. Apply formatting: `shfmt -w -i 0 -bn -ci hardnormly.sh`

   Flag meanings (from research Pattern 2):
   - `-i 0` = tabs (explicit, matches .editorconfig)
   - `-bn` = binary operators (&&, ||, |) may start a line
   - `-ci` = case body indented
   - Do NOT add `-sr` (space redirects) or `-fn` (function brace on new line) — per research recommendation

3. After formatting, run `shfmt -d -i 0 -bn -ci hardnormly.sh` to confirm no diffs remain.

4. Run `shellcheck hardnormly.sh` again to confirm formatting didn't introduce any ShellCheck regressions.

5. If shfmt changes broke any ShellCheck rules (unlikely but possible with whitespace changes), fix them.
  </action>
  <verify>
Run `shfmt -d -i 0 -bn -ci hardnormly.sh` — must exit 0 with no output (no diffs).
Run `shellcheck hardnormly.sh` — must still exit 0.
  </verify>
  <done>hardnormly.sh passes both shellcheck and shfmt -d with zero findings/diffs. The script is now the formatting baseline for all subsequent plans.</done>
</task>

</tasks>

<verification>
```bash
# All three must pass:
shellcheck hardnormly.sh && echo "PASS: shellcheck" || echo "FAIL: shellcheck"
shfmt -d -i 0 -bn -ci hardnormly.sh && echo "PASS: shfmt" || echo "FAIL: shfmt"
test -f .shellcheckrc && test -f .editorconfig && echo "PASS: configs exist" || echo "FAIL: configs missing"
```
</verification>

<success_criteria>
- `shellcheck hardnormly.sh` exits 0 (INFR-01)
- `shfmt -d -i 0 -bn -ci hardnormly.sh` exits 0 (INFR-02)
- `.shellcheckrc` exists with `shell=bash` and optional checks enabled
- `.editorconfig` exists with tab indentation for `.sh` files
- No inline `# shellcheck disable` directives except the documented SC2294 exception on the eval line
</success_criteria>

<output>
After completion, create `.planning/phases/01-infrastructure/01-01-SUMMARY.md`
</output>
